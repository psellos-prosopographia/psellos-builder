"""Schema validation against psellos-spec v0.1."""
from __future__ import annotations

from pathlib import Path


def validate_schema(*, spec_path: Path, input_path: Path) -> None:
    """Stub for validating input datasets against the immutable spec."""
    _ = spec_path
    _ = input_path
